# Production Environment Configuration for Backend
NODE_ENV=production

# Server Configuration
PORT=5001
TRUST_PROXY=true

# MongoDB Configuration - Updated URI with proper options
MONGODB_URI=mongodb+srv://ozodbek:4C6ZNSo5zF15lJvW@cluster0.dlopces.mongodb.net/alibobo?retryWrites=true&w=majority&appName=Cluster0&serverSelectionTimeoutMS=30000&connectTimeoutMS=30000

# Production Features
DEBUG=false
ENABLE_LOGGING=true

# CORS Configuration for Production
CORS_ORIGIN=https://www.aliboboqurilish.uz,https://aliboboqurilish.uz,http://localhost:3000,http://127.0.0.1:3000,http://localhost:3001,http://127.0.0.1:3001

# Rate Limiting (stricter in production)
RATE_LIMIT_MAX=50000

# Clustering (enabled in production for better performance)
ENABLE_CLUSTERING=true

# File Upload Configuration
MAX_FILE_SIZE=50mb
UPLOAD_PATH=./uploads

# Cache Configuration (enabled in production)
CACHE_IMAGES=true
CACHE_MAX_AGE=604800