{
  "name": "alibobo-backend",
  "version": "1.0.0",
  "description": "Clean backend for Alibobo products API",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "start:dev": "cross-env NODE_ENV=development SKIP_INDEX_CREATION=true ENABLE_CLUSTERING=false DEBUG=false node server.js",
    "start:fast": "cross-env NODE_ENV=development SKIP_INDEX_CREATION=true ENABLE_CLUSTERING=false DEBUG=false node server.js",
    "start:ultra": "cross-env NODE_ENV=development SKIP_INDEX_CREATION=true ENABLE_CLUSTERING=false DEBUG=false DISABLE_LOGGING=true MONGODB_BUFFER_COMMANDS=false MONGODB_SOCKET_TIMEOUT=5000 MONGODB_SERVER_SELECTION_TIMEOUT=3000 EXPRESS_TRUST_PROXY=false node server.js",
    "dev": "nodemon server.js",
    "optimize-db": "node scripts/optimize-database.js",
    "db:reset": "node scripts/reset-database.js",
    "db:optimize": "node scripts/optimize-database.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "node",
    "express",
    "mongodb",
    "api",
    "products"
  ],
  "author": "Alibobo Team",
  "license": "MIT",
  "dependencies": {
    "compression": "^1.8.1",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "express-mongo-sanitize": "^2.2.0",
    "express-rate-limit": "^8.0.1",
    "helmet": "^8.1.0",
    "hpp": "^0.2.3",
    "mongoose": "^7.5.0",
    "multer": "^2.0.2",
    "node-fetch": "^3.3.2",
    "socket.io": "^4.8.1"
  },
  "devDependencies": {
    "cross-env": "^10.0.0",
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}
